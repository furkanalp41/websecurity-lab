# SPDX-License-Identifier: MIT
[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "xss-verifier"
version = "1.0.0"
description = "Shared headless-Chromium victim bot for websec-lab XSS labs."
requires-python = ">=3.10"
dependencies = ["playwright==1.49.1"]

[tool.setuptools]
packages = ["xss_verifier"]
