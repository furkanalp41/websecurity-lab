The order of the rows changes when you change `sort`, so your input is reaching
the query. But a single quote does not give you a clean injection here — think
about _where_ in the SQL statement your value lands. It is not inside a string
literal. What kind of SQL clause takes a bare number and decides ordering?
