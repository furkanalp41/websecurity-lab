Your input lands in an `ORDER BY` clause, unquoted. `ORDER BY` accepts more than a
column number — it accepts an arbitrary expression. And the page prints database
errors back to you. So the game is not "escape a string"; it is "put an expression
here that fails in a way that tells me something." What happens if you make
Postgres try to turn text into an integer?
