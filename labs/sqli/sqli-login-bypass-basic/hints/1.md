The dashboard only trusts one thing: a session that says you are an admin. And
the only way to get that session is to pass the login form. So the real target
is not the dashboard — it is the single decision the login endpoint makes about
the two strings you send it. Look closely at how the server _reacts_ to unusual
input in the username field, not just what it accepts.
