Type a single quote (`'`) into the username and submit. If the server answers
with a database error rather than a tidy "invalid credentials", it is building
its SQL query by gluing your text directly into a statement. That is **SQL
injection** in an authentication context — the classic login-bypass. Your input
is being treated as query _code_, so you can change what the query means.
