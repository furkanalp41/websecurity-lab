Send a couple of `POST /search` calls with different bodies (`{"query":{"match":{"message":"login"}}}`,
`{"query":{"match_all":{}}}`, `{"size":1}`). Every field lands verbatim in the ES
`_search` request — the endpoint is a thin passthrough of Query DSL. That is
much more surface than "a search box".

What field would you add to the body to make the search return docs from a
_different_ index than the app intended?
